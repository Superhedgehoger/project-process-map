# P0-05A-T2a-R2F2：Codex R2 Cycle 2 Findings 最小修复

## Repair Task Packet

- Parent Task：P0-05A-T2a
- Previous Repair：P0-05A-T2a-R2F1（当前工作树已有未提交实现）
- Risk：R2
- Base：当前未提交工作树（基准提交 `0598ad482c02876de6f4e804033bdba906b9d6c7`）
- Reviewer Verdict：FAIL
- Worker：Antigravity
- 禁止：额外功能扩展、commit、push

## 目标

只修复本轮独立 Codex R2 Review 确认的五项 blocking findings；保留并完善 R2F1 已有修复，不重做范围外设计。

## Findings 与最小修复边界

1. **Replay 未核对完整权威 action / EvidenceLink**
   - initialize / submit / accept / waive 在 replay 返回 receipt 前，重新鉴权并核对对应 append-only action fact。
   - 核对完整、精确的 EvidenceLink 集合，不能只比较数量；submit action 的 evidence IDs、数量及 receipt payload 必须一致。
   - action/link 缺失、替换、重复或损坏时 fail closed；保留已定义的合法生命周期后续推进兼容规则。

2. **当前新增测试不成立，PASS artifacts 已过期**
   - 修正双连接 CAS 测试或命令检查顺序，使 loser 的稳定错误与既定 CAS 契约一致，不以全局 path lock 掩盖竞态。
   - Grant 过期测试必须 `await` 创建命令，使用可推进的固定 trusted clock，并使用返回的正确 domain version。
   - 修复后重新运行定向和全量 Gate，按当前真实测试数更新报告与 artifacts；不得沿用 8/8、321/321 的旧结果。

3. **Deliverable/action canonical 校验仍接受损坏数据**
   - 对 nullable principal/reason/time 字段做严格类型、canonical 非空及状态互斥校验。
   - accepted 必须有合法 actor/time；waived 必须有合法 actor/time/去空白后非空 reason；其他状态不得串写终态字段。
   - action-specific reason、evidence IDs 的字符串类型/唯一性、`evidenceCount` 与 IDs 数量一致性必须校验。
   - Memory authoritative insertion/read 与 SQLite reopen/first authoritative read 均 fail closed。

4. **Evidence 自然键唯一性与 SQLite v12 schema 校验不足**
   - Memory 与 SQLite 均拒绝不同 link ID 但相同 `(tenant, requirement, sourceType, sourceId)` 的 EvidenceLink。
   - SQLite 增加并验证对应 UNIQUE 约束/索引。
   - reopen 使用 `PRAGMA index_list/index_info` 等确认 requirement key 索引和 evidence 自然键索引确为 UNIQUE；精确验证状态/source/action CHECK 枚举，扩大枚举或把唯一索引替换成普通索引必须 fail closed。

5. **原子性与真实升级证据不完整**
   - initialize / submit / accept / waive 四命令覆盖全部写入边界 failure injection，并逐项断言 aggregate、action/evidence fact、event、outbox、receipt 无部分残留。
   - 构造真实 schema version 11 fixture 验证 v11→v12 升级。
   - 增加旧 v11 binary/旧 schema 识别逻辑拒绝 v12 的直接证据；不得用当前 v12 实现先建库冒充 v11 fixture。

## Required Regression Evidence

- action 删除/替换与等量 EvidenceLink 替换后的 replay 均 fail closed。
- 双连接 CAS 与固定 trusted-clock Grant expiry 测试稳定通过。
- Memory/SQLite 对空 actor、错误 reason 类型、非字符串/重复 evidence ID、count 不一致行为一致。
- 不同 link ID 的重复 evidence 在两后端均拒绝；SQLite 唯一索引降级和 CHECK 枚举扩大后 reopen 拒绝。
- 四命令逐写入边界零残留；真实 v11→v12 与旧版本拒绝 v12。

## Acceptance

- 只处理上述 Reviewer findings，无额外功能扩展。
- 定向测试通过，并重新运行 `pnpm check`、`git diff --check`、凭据扫描。
- 更新 P0-05A-T2a 报告和 artifacts，使其准确描述当前完整工作树、真实测试数量及本轮 FAIL→修复证据。
- 更新 repair task state，但不 commit、不 push；交回 Cindy Gate 后再次进行独立 short-context Codex R2 Review。
