# P0-05A-T2a-R2F11：Codex R2 Cycle 11 Finding 最小修复

- Parent：P0-05A-T2a
- Previous：P0-05A-T2a-R2F10
- Risk：R2
- Base：当前未提交工作树（`0598ad482c02876de6f4e804033bdba906b9d6c7`）
- Verdict：FAIL（1 blocking finding）
- Worker：internal worker（Antigravity control unavailable）
- 禁止范围外扩展、commit、push、T2b

## 仅修复一项

1. **initialize replay 必须绑定初始化时的 node security facts**
   - 当前 replay 只证明 requirement 与当前 node 的 `securityDomainId` / `securityEpoch` 相等，再把当前值传给 exact aggregate 检查；若 node 与 requirement 协同篡改，重放可通过。
   - 使用初始化时已持久化的权威事实（初始化 DomainEvent 的 `originalSecurityDomainId` / `originalSecurityEpoch`）把 requirement、node、receipt replay 与初始安全事实精确绑定；不得把当前协同篡改值当作期望值。
   - 初始化事件必须唯一、属于同 tenant/project/deliverable、为 `project-map.deliverable.initialized`、aggregate v1，并与初始化 actor/time 及初始化 action/receipt 一致；缺失、重复或漂移应 fail closed。
   - 不得改变授权规则；授权仍使用当前 node/security 状态与 persistence trusted clock。
   - 增加 Memory/SQLite 回归：初始化后同时篡改 node 与 requirement 的 security epoch（必要时 domain），保持 receipt/action/event 不变；同命令 replay 必须 `DELIVERABLE_RECORD_CORRUPT`，且失败零新增 durable facts。

## Acceptance

- 保留 R2F1–R2F10 全部行为与证据。
- 运行 `tests/deliverable-evidence.test.ts`、相关 Product API 测试、`pnpm check`、`git diff --check`、凭据扫描、JSON 校验。
- 刷新 artifacts/report/state，真实记录执行通道。
- 不 commit/push；完成后唯一下一步为 Cindy Gate + 新的独立只读 Review Cycle 12。
