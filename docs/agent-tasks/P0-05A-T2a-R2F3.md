# P0-05A-T2a-R2F3：Codex R2 Cycle 3 Findings 最小修复

## Repair Task Packet

- Parent Task：P0-05A-T2a
- Previous Repair：P0-05A-T2a-R2F2
- Risk：R2
- Base：当前未提交工作树（基准提交 `0598ad482c02876de6f4e804033bdba906b9d6c7`）
- Reviewer Verdict：FAIL
- Worker：Antigravity
- 禁止：额外功能扩展、commit、push

## 目标

只修复 Codex R2 Cycle 3 的四项 blocking findings，并补齐直接回归证据。

## Findings 与最小修复边界

1. **首次 accept 未校验权威提交 EvidenceLink 集合**
   - accept 在推进 aggregate 或写任何事实前，读取 authoritative submitted action 与 EvidenceLink。
   - 精确校验 submitted action 的 evidence IDs/count 与当前链接集合一致，并按现有规则重新确认链接/文件仍有效。
   - 链接缺失、等量替换、重复、损坏或 action 漂移必须 fail closed；不得只在 replay 路径校验。

2. **SQLite v12 UNIQUE schema 校验接受 partial index**
   - 校验 requirement key 与 Evidence 自然键唯一索引时读取 `PRAGMA index_list` 的 partial 标志并要求 `partial=0`。
   - 必要时核对 index SQL，确保是覆盖全表的精确 UNIQUE，而不是表达式、partial 或其他弱化形式。
   - 增加将目标 UNIQUE 替换为同列 partial unique index 后 reopen 拒绝的测试。

3. **双连接 CAS 测试仍被进程内全局 path lock 串行化**
   - 不得以移除必要的生产锁或引入不安全并发为代价伪造测试。
   - 使用两个真正独立的执行上下文/进程或等价不共享进程内 `pathLocks` 的 SQLite 连接，构造同一 expectedVersion 的竞争，并证明数据库/CAS 层只有一个成功、另一个稳定失败且无重复事实。
   - 若需最小测试 helper，只限测试或现有 adapter 的安全测试接口，不扩展产品 API。

4. **v11→v12 fixture 仍非真实升级/reopen 证据**
   - 构造完整、真实的 v11 schema fixture（包含 v11 所有既有表/约束/迁移标记及代表性旧数据），不得仅建少数表让 upgrader 当新库补齐。
   - 用当前 `SqlitePersistence` 执行升级，并再次关闭/重新实例化 `SqlitePersistence` reopen。
   - 证明既有代表性数据保留、schema version=12、v12 表/索引有效；继续保留旧 v11 reader 拒绝 v12 的证据。

## Required Regression Evidence

- 删除/替换 submitted EvidenceLink 后首次 accept fail closed，且无 aggregate/action/event/outbox/receipt 部分写入。
- 两个目标 UNIQUE 索引替换成 partial unique index后 reopen 均 fail closed。
- 真正不共享 `pathLocks` 的双连接/双进程 CAS 竞争只有一个 winner。
- 完整 v11 fixture 升级、当前 adapter reopen、旧数据保留及旧 reader 拒绝 v12 全部通过。

## Acceptance

- 只处理上述四项 findings；保留 R2F1/R2F2 修复。
- 更新定向测试并运行 `pnpm check`、`git diff --check`、变更文件凭据扫描。
- 更新报告、artifacts、task/current/checkpoint，准确记录 Cycle 3 FAIL 与真实测试结果。
- 不 commit、不 push；交回 Cindy Gate 后发起新一轮独立 Codex R2 Review。
