# P0-05A-T2a-R2F10：Codex R2 Cycle 10 Findings 最小修复

- Parent：P0-05A-T2a
- Previous：P0-05A-T2a-R2F9
- Risk：R2
- Base：当前未提交工作树（`0598ad482c02876de6f4e804033bdba906b9d6c7`）
- Verdict：FAIL（2 blocking findings）
- Worker：Antigravity
- 禁止范围外扩展、commit、push、T2b

## 仅修复两项

1. **公开 submit / accept / waive 的 HTTP 重试必须保持幂等**
   - `apps/product-api/src/routes/project.ts` 当前每次请求都生成新的 `occurredAtUtc`，而 handler 已将该字段纳入指纹；响应丢失后的同 key 重试因此可能错误返回 payload mismatch。
   - 为已存在的相同 operation/idempotency key 复用原 receipt 的业务时间，且不得让客户端伪造业务时间或把业务时间用于鉴权。
   - 保持不同业务载荷仍返回 `IDEMPOTENCY_KEY_REUSED_WITH_DIFFERENT_PAYLOAD`。
   - 为 submit / accept / waive 补 Product API 层重放回归（至少覆盖首个响应已提交后同 key 同 payload 重试；必要时覆盖竞争窗口）。

2. **initialize replay 必须把全部初始化事实精确绑定到命令与 receipt**
   - `packages/application/src/deliverables/initialize-deliverable-requirement.ts` 的 replay 不得只比 requirementKey/node/project。
   - 精确绑定 title、description、required、规范 acceptedSourceTypes、minCount、reviewerPrincipalId、createdAtUtc/updatedAtUtc、actor、security facts、version/status/terminal null fields，以及 receipt creation time 与规范化命令 `occurredAtUtc`。
   - 初始化 pending@v1 的 action 集必须恰好一条 `initialized`，拒绝额外 action type、缺失、重复或 actor/time/evidence/reason 漂移。
   - 增加 Memory/SQLite coordinated aggregate+receipt tamper 回归（至少 reviewer 替换），并证明失败零新增 durable facts。

## Acceptance

- 保留 R2F1–R2F9 所有安全、重放、CAS、schema、原子性与测试证据。
- 运行 `tests/deliverable-evidence.test.ts`、相关 Product API 测试、`pnpm check`、`git diff --check`、凭据扫描、JSON 校验。
- 刷新 artifacts/report/state，真实记录执行通道与模型。
- 不 commit/push；完成后唯一下一步为 Cindy Gate + 新的独立只读 Review。
