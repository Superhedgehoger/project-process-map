# P0-05A-T2a-R2F7：Codex R2 Cycle 7 Findings 最小修复

- Parent：P0-05A-T2a
- Previous：P0-05A-T2a-R2F6
- Risk：R2
- Base：当前未提交工作树（`0598ad482c02876de6f4e804033bdba906b9d6c7`）
- Verdict：FAIL
- Worker：Antigravity
- 禁止范围外扩展、commit、push

## 仅修复三项

1. **Accept/Waive 终态 replay 精确绑定**
   - 精确要求合法生命周期 action 集。
   - accepted/waived action 的 actor/time/reason 必须与 aggregate、重放 principal、receipt 原始 `createdAtUtc`、规范化命令载荷一致。
   - 无 submitted 的 waive 必须精确为 initialized+waived；有 submitted 则 initialized+submitted+waived。
   - 补 authoritative terminal action + receipt 一致双篡改，以及删除 initialized action 的 Memory/SQLite 回归。

2. **CAS 必须在实际 stale CAS update 前 barrier**
   - release 前的旁路 v1 read 不算证据。
   - 仅测试层协调两个独立 contender：各自实际写事务所用的 aggregate 已读取为 v1，并在紧邻 CAS update 前阻塞；双方 ready 后统一 release。
   - 不增加产品绕锁/权限；loser 仅 version conflict，六类事实唯一。

3. **Accept tamper 快照必须 tenant 全集**
   - 测试 helper 从实际 Memory snapshot 或 SQLite 只读连接抓取 tenant 全部 deliverable evidence links 与 action records，不按 requirement ID 过滤。
   - 连同 events/outbox/receipts 全集做完整 deepEqual；不得新增产品读权限。

## Acceptance

定向、`pnpm check`、diff check、凭据扫描通过；刷新 artifacts/report/state；不 commit/push；交 Cindy Gate + 新 Review。

## 实现记录（Antigravity，本工作树）

- **R2F7-1**：新增 `assertExactTerminalReplayCommandFacts`。Accept replay 的动作集必须精确为 `initialized+submitted+accepted`；Waive 未提交时必须精确为 `initialized+waived`，提交后必须精确为 `initialized+submitted+waived`。终态动作的 actor/time/reason 同时绑定 aggregate、重放 principal、receipt 原始 `createdAtUtc` 与规范化命令 reason。新增 Memory/SQLite 双后端回归，覆盖权威终态 action + receipt 一致双篡改，以及删除 initialized action。
- **R2F7-2**：CAS 测试改为两个独立子进程均运行真实 Product submit handler。仅测试层包装 SQLite 方法，使两个真实 handler 写事务都读取 v1，并在真实 CAS UPDATE 紧邻边界写 ready marker 后阻塞；父进程确认两方 marker 均为 v1 才统一 release。loser 在 winner commit 后执行 stale `WHERE version=1` CAS，仅返回 `DELIVERABLE_VERSION_CONFLICT`；六类 durable facts 唯一性保留。未增加产品 hook、权限或绕锁入口。
- **R2F7-3**：`deliverable-record-snapshot.ts` 改为 tenant 全集快照。Memory 直接抓实际 `snapshot()` 中该 tenant 全部 EvidenceLink/Action/Event/Outbox/Receipt；SQLite 用单独只读连接读取该 tenant 对应五类全表记录。Requirement ID 不再参与过滤，失败前后完整集合 `deepEqual`。

## 验证（Antigravity，本工作树）

- 定向 `tests/deliverable-evidence.test.ts`：25/25 PASS。
- 8 个目标套件：147/147 PASS。
- `pnpm check`：338/338 PASS（typecheck、全量 tests、Huly image lock 14 images linux/arm64、Huly extension verify upstream `ccefccd8d0361d3c8612d508071b777aa833826d`）。
- `git diff --check`：PASS。
- R2F7 实现文件凭据/私钥扫描：PASS。
- 状态：IMPLEMENTED（未 commit/push）；交 Cindy Gate 与新独立 Review。
