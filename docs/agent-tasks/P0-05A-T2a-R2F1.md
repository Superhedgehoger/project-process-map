# P0-05A-T2a-R2F1：Codex R2 Findings 最小修复

## Repair Task Packet

- Parent Task：P0-05A-T2a
- Risk：R2
- Base：当前未提交工作树（基准提交 `0598ad482c02876de6f4e804033bdba906b9d6c7`）
- Reviewer Verdict：FAIL
- Worker：Antigravity
- 禁止：额外功能扩展、commit、push

## 目标

只修复本轮独立 Codex R2 Review 已确认的 blocking findings，并补齐对应回归证据。

## Findings 与最小修复边界

1. **安全域迁移 / Security Root 漏掉 Deliverable**
   - 将 DeliverableRequirement 纳入初始和 resumable migration inventory、manifest/batch/rollback 的既有事务与 CAS 路径。
   - Security Root 空叶节点检查必须拒绝已有 deliverable 的节点；domain-id legacy reference 检查必须包含 deliverable。
   - 不引入新的迁移模型或产品能力。

2. **Asset ID 枚举错误码**
   - 对不存在、删除、跨 project/node/domain/epoch 的不可知 Asset 统一 fail-closed 为 `EVIDENCE_NOT_FOUND` / 既有 404 产品语义。
   - 只有在 scope 已确认匹配后，才可暴露 lifecycle unavailable。

3. **Replay 权威校验不完整**
   - 四个命令 replay 返回 receipt 前重新鉴权，并校验 authoritative requirement、node、domain/epoch、对应 action、EvidenceLink 集合、文件可用性与完整 payload fingerprint。
   - 漂移、缺失、损坏必须 fail closed；不得简单把后续合法状态推进误判为损坏，需明确 receipt 与当前权威生命周期兼容规则并用测试固定。

4. **Deliverable canonical / SQLite corruption fail-closed 不完整**
   - 校验所有 nullable 字段、Security Domain 字段、删除时间及 UTC 时间。
   - 增加状态不变量：accepted 必须有 actor/time；waived 必须有 actor/time/非空 reason；互斥终态字段不得串写。
   - SQLite reopen 或首次权威读取对损坏记录稳定拒绝。

5. **测试漏口**
   - 补 Memory/SQLite 的非空 Security Domain、Grant 撤销/过期、domain/epoch/迁移交集矩阵。
   - 补真实 SQLite 双连接 CAS 竞态。
   - 补 v11→v12、reopen、行/Schema corruption fail-closed。
   - 补 replay 权限撤销、状态兼容推进、权威/Evidence 漂移。
   - 补 initialize/submit/accept/waive 全写入边界 failure injection，并断言 aggregate/fact/event/outbox/receipt 零部分残留。

## Acceptance

- 所有 Reviewer findings 有直接回归测试。
- Memory / SQLite 行为一致。
- 定向测试通过。
- `pnpm check`、`git diff --check`、凭据扫描通过。
- 更新 P0-05A-T2a 报告与 artifacts，保留 Reviewer FAIL 及修复证据。
- 不 commit、不 push；完成后交回 Cindy 重新 Gate，再进行新的独立 Codex R2 Review。
