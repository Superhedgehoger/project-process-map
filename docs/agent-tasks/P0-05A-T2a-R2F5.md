# P0-05A-T2a-R2F5：Codex R2 Cycle 5 Findings 最小修复

## Repair Task Packet

- Parent Task：P0-05A-T2a
- Previous Repair：P0-05A-T2a-R2F4
- Risk：R2
- Base：当前未提交工作树（基准提交 `0598ad482c02876de6f4e804033bdba906b9d6c7`）
- Reviewer Verdict：FAIL
- Worker：Antigravity
- 状态：IMPLEMENTED（本工作树）
- 禁止：额外功能扩展、commit、push

## 目标

只修复 Codex R2 Cycle 5 的四项 blocking findings。

## Findings 与最小修复边界

1. **Submit replay 未绑定 EvidenceLink sourceType**
   - replay 将权威 links 的完整 `(sourceType, sourceId)` 集合与原命令/receipt/action 精确比较，而非只比 sourceId。
   - 对每个权威 link 重新执行 accepted-source 及 ProcessRecord unsupported 约束；任何 `file → process_record` 篡改稳定 fail closed。
   - 增加同时篡改 authoritative link 与 receipt sourceType 的回归测试。

2. **跨进程测试仍未证明 CAS 竞争且事实唯一性断言不全**
   - 增加跨进程 ready/release barrier，使两个独立执行上下文都准备完毕后进入同一竞争窗口；双方 outcome 均以 settled 形式采集。
   - 不得因主进程先完成而退化成顺序读取新版；loser 只允许 `DELIVERABLE_VERSION_CONFLICT`，不接受 busy/locked。
   - 精确断言 aggregate 最终版本/状态，以及 evidence/action/event/outbox/receipt 六类事实恰好对应一个 winner、无重复。

3. **Frozen v11 guard 仍是新写替身**
   - 从 base `0598ad482c02876de6f4e804033bdba906b9d6c7` 冻结实际 v11 reader/startup guard 代码及其原始打开、关闭、错误契约，保留可验证来源说明。
   - 测试执行该真实 frozen guard/adapter 路径，不得仅调用新写的 MAX(version)+throw helper。
   - 保持 frozen v11 schema 独立建库、当前 adapter upgrade/reopen、旧数据保留证据。

4. **Accept tamper 零残留断言不精确**
   - delete-link 后精确断言 links 为空；replace-link 后精确等于篡改集合。
   - actions 精确等于原 initialized + submitted 的 IDs/types/content，不得只断言长度和存在 submitted。
   - 继续验证 aggregate、event、outbox、receipt 无 accept 残留，Memory/SQLite 一致。

## Acceptance

- 仅处理四项 findings，保留 R2F1-R2F4 其他修复。
- 定向、`pnpm check`、`git diff --check`、凭据扫描通过。
- 更新报告/artifacts/task/current/checkpoint，记录 Cycle 5 FAIL 与真实结果。
- 不 commit、不 push；交回 Cindy Gate 后再次独立 Review。

## 实现记录（Antigravity，本工作树）

- **R2F5-1**：`packages/application/src/deliverables/submit-deliverable-evidence.ts` replay 分支对每个 replay 证据项重新执行 accepted-source/ProcessRecord 约束，并将权威 links 的完整 `(sourceType, sourceId)` 集合与原命令/receipt 精确比较（不匹配 → `DELIVERABLE_RECORD_CORRUPT`）。新增 `R2F5-1 (memory/sqlite)` 双篡改回归测试（`tests/deliverable-evidence.test.ts`）。
- **R2F5-2**：`tests/helpers/cas-independent-worker.ts` 增加跨进程 ready/release barrier（父/子 ready 标记，双方 settled 后进入同一竞争窗口）；主进程侧 submit 承诺被包裹以采集其 settled 的 `DELIVERABLE_VERSION_CONFLICT` loser 结果。`R2F1-4` 第 4 节竞争后对 aggregate/evidence/action/event/outbox/receipt 六类事实做精确唯一性断言。
- **R2F5-3**：新增冻结模块 `packages/adapters/src/sqlite/schema/v11-reader.ts`（`FrozenV11SqlitePersistenceReader`，逐字转录基准提交 0598ad4 的 v11 代 `SqlitePersistence` 启动路径与 fail-closed close 契约，模块头部保留来源说明）。`R2F2-4/5` 与 `R2F3-4` 旧 reader 拒绝证据改为执行该冻结 reader 原始代码路径，并保留 frozen v11 schema 独立建库、v11→v12 升级/reopen/旧数据保留证据。
- **R2F5-4**：`R2F3-1` 零残留断言精确化：delete-link → links 恰好为空；replace-link → 恰好等于篡改集合；actions 用 `deepEqual` 精确等于原 initialized + submitted 记录（id/类型/actor/evidenceIds）；保持 aggregate submitted@v2 与 event/outbox/receipt 零残留，Memory/SQLite 一致。

- 验证：定向 `tests/deliverable-evidence.test.ts` 22/22（连续 3 次）；8 个目标套件 144/144；`pnpm check` 335/335；`git diff --check` 通过；变更文件凭据扫描无命中。
