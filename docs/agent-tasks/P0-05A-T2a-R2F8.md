# P0-05A-T2a-R2F8：Codex R2 Cycle 8 Finding 最小修复

- Parent：P0-05A-T2a
- Previous：P0-05A-T2a-R2F7
- Risk：R2
- Base：当前未提交工作树（`0598ad482c02876de6f4e804033bdba906b9d6c7`）
- Verdict：FAIL（1 blocking finding）
- Worker：Antigravity
- 禁止范围外扩展、commit、push

## 唯一修复项

**CAS 证据必须在同一实际物理写事务内持有 stale v1 并执行 CAS UPDATE**

- 删除 v1 marker 与 CAS UPDATE 之间的 `ROLLBACK/BEGIN IMMEDIATE` 重启路径。
- 两个独立 contender 必须在各自同一物理事务中读取 v1、到达紧邻 CAS UPDATE 的 barrier，并在 release 后于该事务内执行真实 CAS。
- 测试必须记录/断言事务身份从 v1 read 到 UPDATE 未变化。
- loser 仅允许 `DELIVERABLE_VERSION_CONFLICT`，六类事实唯一性保持。
- 若 SQLite 锁模型不允许两个 `BEGIN IMMEDIATE` 同时持有，采用不提前抢 writer lock 的合法事务/协调设计；不得通过跨事务 stale 对象合成冲突，不得增加生产绕锁或权限。

## Acceptance

定向、`pnpm check`、diff check、凭据扫描通过；刷新 artifacts/report/state；不 commit/push；交 Cindy Gate + 新独立 Review。
