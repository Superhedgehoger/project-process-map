# P0-05A-T2a-R2F12：Codex R2 Cycle 12 Findings 最小修复

- Parent：P0-05A-T2a
- Previous：P0-05A-T2a-R2F11
- Risk：R2
- Base：当前未提交工作树（`0598ad482c02876de6f4e804033bdba906b9d6c7`）
- Verdict：FAIL（2 blocking findings）
- Worker：internal worker（Antigravity control unavailable）
- 禁止范围外扩展、commit、push、T2b

## 仅修复两项

1. **迁移后 submit replay 不得硬编码 version 2**
   - submit 首次成功可能发生在安全迁移导致 requirement version 提升之后。
   - replay 应把 authoritative submitted version 精确绑定到 receipt 与原命令 `expectedVersion + 1`，不得硬编码 `2`；后续合法生命周期推进仍应返回 stale/version conflict。
   - 增加 Memory/SQLite 回归：pending requirement 经合法 security migration 变为 v2，以 expectedVersion=2 submit 成功到 v3，同 key 同 payload replay 成功；后续生命周期推进仍不误 replay。

2. **submit / accept / waive replay 时间必须由指纹命令与唯一 event/outbox 共同锚定**
   - 不得使用可协调篡改的 receipt `createdAtUtc` 代替规范化 replay command `occurredAtUtc`。
   - submit：command、receipt createdAt、submitted action、links、receipt view 及唯一 submitted DomainEvent 的时间/actor/aggregate/version/payload 必须一致；对应 outbox payload 必须与 event 一致（允许 outbox 状态/租约随 worker 合法变化，不把运行态字段硬编码）。
   - accept / waive：command、receipt createdAt、terminal aggregate/action/receipt view 及唯一 terminal DomainEvent 的时间/actor/aggregate/version/reason payload 必须一致；对应 outbox payload 与 event 一致。
   - event/outbox 缺失、重复或漂移必须 fail closed；鉴权仍只用 current trusted clock。
   - 增加 Memory/SQLite 协调篡改回归：保持 fingerprint 与 replay command T1，改 receipt createdAt 及相应 aggregate/action/link/receipt 时间为 T2，但保留原 event/outbox T1；replay 必须 `DELIVERABLE_RECORD_CORRUPT` 且零新增 durable facts。

## Acceptance

- 保留 R2F1–R2F11 全部行为与证据。
- 运行 deliverable、Product API 定向测试、`pnpm check`、`git diff --check`、凭据扫描、JSON 校验。
- 刷新 artifacts/report/state，真实记录执行通道。
- 不 commit/push；完成后唯一下一步为 Cindy Gate + 新的独立只读 Review Cycle 13。
