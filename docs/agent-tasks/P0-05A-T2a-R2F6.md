# P0-05A-T2a-R2F6：Codex R2 Cycle 6 Findings 最小修复

## Repair Task Packet

- Parent：P0-05A-T2a
- Previous：P0-05A-T2a-R2F5
- Risk：R2
- Base：当前未提交工作树（`0598ad482c02876de6f4e804033bdba906b9d6c7`）
- Verdict：FAIL
- Worker：Antigravity
- 禁止：范围外扩展、commit、push

## 仅修复以下四项

1. **Submit replay 全事实一致性**
   - action 集必须精确为 initialized + submitted。
   - submitted actor 必须等于命令 principal；每条 link 的 submitter/time 与 submitted action 一致；canonical link ID 与完整 `(sourceType,sourceId)` pair 一致。
   - action/receipt/link 同时篡改 actor、submitter、time、ID 时均 fail closed；补 Memory/SQLite 双篡改测试。

2. **Accept/Waive replay 的 ProcessRecord/证据语义重验**
   - accept replay 复用首次 accept 的完整权威提交语义校验。
   - waive replay：存在 submitted action 时执行同样文件证据校验；无 submitted action 时 links 必须为空。
   - 非 file、未接受 source、失效/跨界 Asset 均稳定拒绝；补 link+receipt 同改为 process_record 的 accept/waive replay 测试。

3. **CAS barrier 必须证明双方同时持有 stale v1**
   - 使用三阶段协调：双方各自读取/记录同一 v1 stale version 并 ready；父进程确认两方 ready 后才 release 两个 contender。
   - 不得让 worker 在 main submit 启动前完成；双方 outcomes settled，loser 仅 `DELIVERABLE_VERSION_CONFLICT`。
   - 不引入生产绕锁/权限；保留六类事实唯一性断言。

4. **Accept tamper 完整快照零残留**
   - 篡改后、accept 前保存实际 mutator backend 的完整 links/actions/events/outbox/receipts 基线。
   - 失败后对完整记录集合 deepEqual，不做字段投影或定向 ID 过滤。
   - receipt 全集检查仅放测试辅助层，不新增产品读权限；Memory/SQLite 一致。

## Acceptance

定向、`pnpm check`、`git diff --check`、凭据扫描通过；更新 artifacts/report/state；不 commit/push；交 Cindy Gate + 新独立 Review。

## 实现记录（Antigravity，本工作树）

- **R2F6-1**：`packages/application/src/deliverables/initialize-deliverable-requirement.ts` 新增 `assertExactSubmitReplayCommandFacts`：权威动作集合必须恰好为 initialized + submitted；submitted 动作的 actor 必须等于重放命令 principal；每条 link 的 submitter/time 必须等于 submitted 动作；每个 link ID 必须是 `link:<reqId>:<sourceType>:<sourceId>` 的规范形式；receipt 中的动作/链接记录也必须满足同样的命令一致性（submitted 时间绑定 receipt 原始记录时间 `previous.createdAtUtc`，而非重放命令时间戳）。`submit-deliverable-evidence.ts` 回放分支调用该助手。新增 `R2F6-1 (memory/sqlite)` 双后端回归测试：同时篡改 submitted 动作 actor+时间、link submitter+时间+ID 以及 receipt 记录（保持新值内部一致），原载荷重放提交稳定 fail closed（`DELIVERABLE_RECORD_CORRUPT`）。
- **R2F6-2**：`packages/application/src/deliverables/accept-deliverable.ts` 新增 `validateReplaySubmissionFacts`——复用首次 accept 的完整权威提交语义（`validateAuthoritativeSubmission`）于动作/链接记录集的提交部分，容忍终端 accepted/waived 动作；accept 回放分支改为调用它，替代旧的逐链接 asset 可用性循环。`waive-deliverable.ts` 回放分支：存在 submitted 动作时调用同一助手；无 submitted 动作时链接集合必须为空（否则 `DELIVERABLE_RECORD_CORRUPT`）。新增 `R2F6-2 (memory/sqlite, accept+waive)` 回归测试：link+receipt 同改为 `process_record` 时，accept/waive 回放均稳定拒绝。
- **R2F6-3**：`tests/helpers/cas-independent-worker.ts` 升级为三阶段跨进程 barrier：Phase 1 双方各自通过自己的 persistence 读取并记录同一 stale v1 版本（父进程 `persistence.read`；子进程脚本内 `persistence.read`，经 `.worker.stale` 标记文件上报观测值）；Phase 2 父进程 `createParentReadyMarker` + `waitForChildReadyMarker`（现在返回子进程观测到的 stale 版本）；Phase 3 仅在双方 stale 观测都被父进程确认（均 = 1）后，`writeCasReleaseMarker` 释放两个 contender，子进程在 release 标记处解除阻塞，父进程随即启动自己的 submit。`R2F1-4` 第 4 节竞争前断言 `mainStaleVersion === 1`、`childStaleVersion === 1`、`releasePhaseReached`；双方 outcomes 仍以 settled 形式采集，loser 仅 `DELIVERABLE_VERSION_CONFLICT`；六类事实唯一性断言保留。未引入任何生产绕锁/权限改动。
- **R2F6-4**：新增测试层助手 `tests/helpers/deliverable-record-snapshot.ts`（`captureSnapshot` / `assertNoRecordResidue`）：篡改后、accept 前在**实际 mutator backend** 上捕获完整 links/actions/events/outbox/receipts 记录集合（无字段投影、无定向 ID 过滤；receipt 全集读取仅存在于该助手层，未新增任何产品读权限；Memory 走 `snapshot()`，SQLite 走独立只读 `DatabaseSync` 句柄）。`R2F3-1` 改为捕获该基线，对失败后完整记录集合 `deepEqual`，Memory/SQLite 一致；原有精确 links/actions 断言保留。

## 验证（Antigravity，本工作树）

- 定向 `tests/deliverable-evidence.test.ts` 24/24（连续 3 次，含 `R2F6-1`、`R2F6-2` 与三阶段 CAS barrier 竞争）。
- 8 个目标套件（`deliverable-evidence`、`task-upgrade-compatibility`、`security-migration-batch`、`security-root`、`security-grant`、`node-owner`、`task-reviewer-resolution`、`product-api`）：146/146 PASS。
- `pnpm check`：337/337 PASS（typecheck、tests、Huly image lock 14 images linux/arm64、Huly extension verify upstream `ccefccd8d0361d3c8612d508071b777aa833826d`）。
- `git diff --check` 通过。
- 变更文件凭据/私钥扫描：仅命中既有的敏感字段拒绝清单与测试 fixture（无真实密钥/凭据）。

- 状态：IMPLEMENTED（本工作树）；未 commit/push。
