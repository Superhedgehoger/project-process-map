# P0-05A-T2a-R2F4：Codex R2 Cycle 4 Findings 最小修复

## Repair Task Packet

- Parent Task：P0-05A-T2a
- Previous Repair：P0-05A-T2a-R2F3
- Risk：R2
- Base：当前未提交工作树（基准提交 `0598ad482c02876de6f4e804033bdba906b9d6c7`）
- Reviewer Verdict：FAIL
- Worker：Antigravity
- 禁止：额外功能扩展、commit、push

## 目标

只修复 Codex R2 Cycle 4 的三项 blocking findings，并提供不依赖循环自证的直接回归证据。

## Findings 与最小修复边界

1. **CAS 证据仍被同步 SQLite writer lock 掩盖，测试旋钮暴露于生产适配器**
   - 删除公开 `SqlitePersistenceOptions.testExecutionId` 及生产适配器中的绕锁分支。
   - 用 worker thread、子进程或等价真正独立执行上下文运行第二个真实 `SqlitePersistence`，使两个调用不共享进程内 `pathLocks` 且不会因同一事件循环的同步 `DatabaseSync` 阻塞而伪造竞争。
   - loser 必须到达版本检查并稳定返回 `DELIVERABLE_VERSION_CONFLICT`；不得把 `SQLITE_BUSY/locked` 接受为 CAS 证明。
   - 断言恰好一个 winner，且 aggregate/action/evidence/event/outbox/receipt 无重复。

2. **v11 fixture 与旧 reader 循环自证**
   - 不得用当前 v12 adapter 建库后删除 v12 表/标记来生成 v11 fixture。
   - 使用冻结的 v11 schema SQL、仓库中可验证的历史 v11 fixture/迁移构造器，或冻结的 v11 adapter 代码，独立创建完整 v11 schema 并种入代表性数据。
   - 用当前 adapter 执行 v11→v12，关闭并重新实例化当前 `SqlitePersistence` reopen，验证旧数据保留和 v12 schema。
   - 旧版本拒绝证据必须执行冻结的真实 v11 reader/version guard 逻辑，不得用测试内仅查询 MAX(version) 后手工抛错的替身。

3. **首次 accept 零残留测试检查错误实例且未覆盖 event/outbox/receipt**
   - Memory 分支必须从实际执行 accept 的 `mutatorPersistence` 读取失败后状态。
   - Memory/SQLite 均验证 aggregate 保持 submitted，links/actions 不变，并按本次 command/event/outbox/receipt 身份确认没有新 event、outbox、receipt。
   - 删除和等量替换 EvidenceLink 两种篡改都覆盖，测试不得仅凭原始未篡改 fixture 通过。

## Acceptance

- 仅处理以上三项 findings，保留 R2F1-R2F3 其余修复。
- 定向测试通过；`pnpm check`、`git diff --check`、变更文件凭据扫描通过。
- 更新报告、artifacts、task/current/checkpoint，准确记录 Cycle 4 FAIL 和真实验证结果。
- 不 commit、不 push；交回 Cindy Gate 后执行新的独立 Codex R2 Review。
