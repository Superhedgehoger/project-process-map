# P0-05A-T2a-R2F13：Codex R2 Cycle 13 Findings 最小修复

- Parent：P0-05A-T2a
- Previous：P0-05A-T2a-R2F12
- Risk：R2
- Base：当前未提交工作树（`0598ad482c02876de6f4e804033bdba906b9d6c7`）
- Verdict：FAIL（3 blocking findings）
- Worker：internal worker（Antigravity control unavailable）
- 禁止范围外扩展、commit、push、T2b

## 仅修复三项

1. **动作 replay 必须绑定 aggregate `updatedAtUtc`**
   - submit / accept / waive replay 均要求权威 requirement 的 `updatedAtUtc` 等于规范化 command `occurredAtUtc`。
   - accept / waive 还必须继续与 `acceptedAtUtc` / `waivedAtUtc` 一致。
   - 增加 Memory/SQLite aggregate-only `updatedAtUtc` 篡改回归与零新增 durable facts 断言。

2. **accept / waive replay 版本必须绑定原命令**
   - 权威 requirement 与 receipt view 的 terminal version 必须精确等于 `command.expectedVersion + 1`。
   - 增加 Memory/SQLite 协调改写 aggregate、receipt、event、outbox version 的回归；重放必须 version conflict 且零新增 durable facts。

3. **post-migration submit 回归必须走合法迁移闭环**
   - 用正式 security migration inventory、batch、verification、readiness evidence 与 commit handler 完成敏感域到 public 的迁移。
   - 精确断言 node/asset/requirement 的 target domain、epoch 与 requirement v2，再 submit 到 v3 并同键重放成功。
   - 后续 accept 推进到 v4 后，旧 submit replay 必须 stale/version conflict。

## Acceptance

- 保留 R2F1–R2F12 全部行为与证据。
- 运行 deliverable、Product API 定向测试、`pnpm check`、`git diff --check`、凭据扫描、JSON 校验。
- 刷新 artifacts/report/state，真实记录执行通道。
- 不 commit/push；完成后唯一下一步为新的独立只读 Review Cycle 14。
