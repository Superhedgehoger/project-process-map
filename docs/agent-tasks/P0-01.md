# P0-01：核验 Huly 基线

## 允许修改

- `.huly-baseline.json`
- `schemas/huly-baseline.schema.json`
- `docs/phase0-status.md`
- `docs/adr/ADR-001-huly-baseline.md`
- 与基线核验直接相关的 `tools/` 和测试

## 目标

1. 复核 Platform `v0.7.426` 是否解析到锁定的完整 commit。
2. 复核 Selfhost 的锁定 commit 与该 Platform 版本兼容。
3. 从锁定源码生成 CycloneDX SBOM 初稿和许可证清单。
4. 在干净目录执行官方 quick setup，记录镜像 digest、日志、耗时与健康状态。
5. 未完成部署和合规复核前，不得把 decision 改成 `go`。

## 禁止事项

- 不使用 `latest`、浮动分支或短 SHA。
- 不提交登录信息、`.env`、Docker volume 数据或用户数据。
- 不修改 Huly 核心代码。
- 不决定生产数据库、Task 权威来源或敏感 ACL。

## 验收

- `pnpm check`
- `pnpm huly:verify`
- 干净环境部署记录可复现。
