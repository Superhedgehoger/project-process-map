# P0-05A-T2a-R2F9：Codex R2 Cycle 9 Findings 最小修复

- Parent：P0-05A-T2a
- Previous：P0-05A-T2a-R2F8
- Risk：R2
- Base：当前未提交工作树（`0598ad482c02876de6f4e804033bdba906b9d6c7`）
- Verdict：FAIL（2 blocking findings）
- Worker：Antigravity
- 禁止范围外扩展、commit、push

## 仅修复两项

1. **完整载荷指纹必须覆盖 occurredAtUtc**
   - initialize / submit / accept / waive 四命令的幂等指纹纳入规范化 `occurredAtUtc`。
   - 同幂等键不同业务时间稳定返回既有 idempotency payload mismatch 错误。
   - 保持 trusted clock 仅用于鉴权；occurredAtUtc 仍只是业务记录输入。
   - 补四命令 Memory/SQLite 回归；评估并明确当前切片未提交、无需兼容旧 deliverable receipt 的事实，禁止影响既有 Task 等其他 receipt generation。

2. **SQLITE_BUSY_SNAPSHOT 映射不得泄漏生产适配器**
   - 删除生产 `SqlitePersistence` 中为测试加入的 517→`DELIVERABLE_VERSION_CONFLICT` 泛化映射。
   - 仅在受控 CAS 测试 helper 中处理 517；处理前必须读取/证明目标 requirement version 已从 stale v1 变化，否则不得当作版本冲突。
   - 保留 Cycle8 同事务 stale-read/CAS、transaction identity、六类事实唯一性证据；不得添加生产绕锁或权限。

## Acceptance

定向、`pnpm check`、diff check、凭据扫描通过；刷新 artifacts/report/state；不 commit/push；交 Cindy Gate + 新独立 Review。
